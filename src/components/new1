/* NavbarStyles.css */

/* Navbar container */
nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    background-color: #007aff;
    color: white;
  }
  
  /* Logo link */
  #logo-35 {
    fill: white;
  }
  
  /* Navbar menu */
  #navbar {
    display: flex;
    list-style: none;
    gap: 1rem;
  }
  
  /* Navbar menu items */
  #navbar li {
    position: relative;
  }
  
  /* Navbar links */
  #navbar li a {
    color: white;
    text-decoration: none;
  }
  
  /* Navbar mobile icon */
  #mobile {
    display: none;
    color: white;
    cursor: pointer;
  }
  
  /* Active state for mobile menu */
  #navbar.active {
    display: flex;
    flex-direction: column;
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background-color: #007aff;
    padding: 1rem;
  }
  
  /* Active state for mobile menu items */
  #navbar.active li {
    margin-bottom: 1rem;
  }
  
  /* Login and register menus */
  .nav-main {
    display: none;
  }
  
  .nav-main.show {
    display: flex;
    flex-direction: column;
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background-color: #007aff;
    padding: 1rem;
  }
  
  /* Login and register menu items */
  .nav-main div {
    margin-bottom: 1rem;
  }
  
  /* Login and register menu links */
  .nav-main div a {
    color: white;
    text-decoration: none;
  }
  
  /* Mobile icon styles */
  #mobile i {
    font-size: 1.5rem;
  }
  
  @media screen and (max-width: 768px) {
    /* Show mobile icon in mobile view */
    #mobile {
      display: block;
    }
  
    /* Hide desktop login and register menus */
    .login,
    .ouy {
      display: none;
    }
  }
  